apiVersion: v1
kind: Service
metadata:
  name: "service-demo"
  namespace: "demo-game"
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app: "2048"
